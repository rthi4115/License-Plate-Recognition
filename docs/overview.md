# Project Overview

This repository contains code and resources for license plate recognition using OpenCV and related Python libraries. Key scripts are at the repository root (`main.py`, `test_camera.py`).
